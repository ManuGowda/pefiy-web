// Vite server helpers removed for frontend-only project
// The server-side Vite middleware is no longer needed. Use the vite CLI for dev/build/preview.

export function log() {}
export async function setupVite() { return null as any; }
export function serveStatic() {}
